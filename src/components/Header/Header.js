import styles from './Header.module.css';



const Header = () => {

    return <header className={styles.header}>
        <div>
        <h1 className={styles.title}>Fitango YouX</h1>
        </div>
        
    </header>
};

export default Header;